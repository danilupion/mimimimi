apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimimimi-web.fullname" . }}
  labels:
    {{- include "mimimimi-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "mimimimi-web.fullname" . }}
